# Run docker build
docker-compose up -d --build

docker compose down -v
docker compose up --build

#admin 
username - min
email - 
password - ****

#local run
docker-compose -f docker-compose.yml -f docker-compose.local.yml up
docker-compose -f docker-compose.yml -f docker-compose.local.yml up --build

docker-compose -f docker-compose.yml -f docker-compose.local.yml build --no-cache

docker-compose -f docker-compose.yml -f docker-compose.local.yml exec web python manage.py shell
docker-compose -f docker-compose.yml -f docker-compose.local.yml exec web python manage.py createsuperuser

docker-compose -f docker-compose.yml -f docker-compose.local.yml down

#prod run
docker-compose -f docker-compose.yml -f docker-compose.prod.yml up --build -d


docker-compose -f docker-compose.yml -f docker-compose.prod.yml down --remove-orphans
docker-compose -f docker-compose.yml -f docker-compose.prod.yml build --no-cache
docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d

docker-compose -f docker-compose.yml -f docker-compose.local.yml exec web python manage.py makemigrations
docker-compose -f docker-compose.yml -f docker-compose.local.yml exec web python manage.py migrate
docker-compose -f docker-compose.yml -f docker-compose.local.yml restart web

# from shell, add subdomain for local test. without it , it shows error - no fallback setup yet.
from orders.models.tenant import Tenant
# If you’re using localhost without subdomain
Tenant.objects.create(name="Local Tenant", subdomain="127-0-0-1")